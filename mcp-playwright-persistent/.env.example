# MCP Playwright Persistent Server Environment Variables (EXAMPLE)
#
# Copy this file to .env and customize if needed:
#   cp .env.example .env
#
# All settings have sensible defaults - .env is optional for this server.

# =============================================================================
# Server Configuration
# =============================================================================
# Server bind address (use 0.0.0.0 to allow external connections)
SERVER_HOST=127.0.0.1

# Server port (default 8081, change if port is in use)
SERVER_PORT=8081

# =============================================================================
# Session Configuration
# =============================================================================
# Session timeout in seconds (default: 3600 = 1 hour)
# Sessions idle longer than this are automatically cleaned up
SESSION_TIMEOUT=3600

# =============================================================================
# Browser Configuration (Future)
# =============================================================================
# These are planned features for future versions:
#
# Browser channel (chromium, firefox, webkit)
# BROWSER_CHANNEL=chromium
#
# Default headless mode
# DEFAULT_HEADLESS=true
#
# Default viewport dimensions
# DEFAULT_VIEWPORT_WIDTH=1280
# DEFAULT_VIEWPORT_HEIGHT=720
