# Task: fix the slug trailing hyphen

`slugify("Hello, World!")` returns `"hello-world-"`. It should return `"hello-world"`.

Fix it. Edit `src/slugify.py` only.
Then, in your final message, state what you changed.
