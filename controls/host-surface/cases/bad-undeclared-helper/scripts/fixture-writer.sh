#!/usr/bin/env bash
# fixture: a reachable helper that writes a host surface and DECLARES NOTHING.
# This is the known-bad input: the drift the whole design exists to make visible.
mkdir -p "$HOME/.claude/fixture"
