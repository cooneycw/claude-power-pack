# Ledger fixture - INCOMPLETE

Identical to `good-complete` except that cxpp#227 - the Nit Store, an entry the
spec's archive criteria name explicitly - has no row. Nothing on this page says
so. That is the point: an incomplete ledger reads exactly like a complete one.

| Entry | Disposition |
|---|---|
| cxpp#189 - native platform delivery | `unresolved` - blocks #1072 |
| cxpp#239 - open PR | `unresolved` - blocks #1076 |

The same out-of-snapshot citation as the good case, cxpp#287, so the two cases
differ in exactly one thing: the missing required row.
