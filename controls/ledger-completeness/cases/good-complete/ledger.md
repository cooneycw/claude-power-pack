# Ledger fixture - COMPLETE

Every entry in this case's snapshot has a row.

| Entry | Disposition |
|---|---|
| cxpp#189 - native platform delivery | `unresolved` - blocks #1072 |
| cxpp#227 - Nit Store | `unresolved` - blocks #1076 |
| cxpp#239 - open PR | `unresolved` - blocks #1076 |

An out-of-snapshot citation, present on purpose: cxpp#287 merged before the
baseline. The gate must NOT treat an extra number as a finding, or correct prose
turns it red and it gets switched off.
