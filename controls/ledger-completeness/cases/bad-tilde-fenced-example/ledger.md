# Ledger fixture - THE ONLY ROW IS AN EXAMPLE

cxpp#227's only appearance is inside a TILDE-fenced illustration. A fence rule
that recognises backticks alone does not strip it, so a worked example accounts
for a real obligation.

| Entry | Disposition |
|---|---|
| cxpp#189 - native platform delivery | `unresolved` - blocks #1072 |
| cxpp#239 - open PR | `unresolved` - blocks #1076 |

How a row should look:

~~~markdown
| cxpp#227 - Nit Store | `unresolved` - blocks #1076 |
~~~

Differs from `good-complete` in one thing: 227's row is illustrative rather
than real.
