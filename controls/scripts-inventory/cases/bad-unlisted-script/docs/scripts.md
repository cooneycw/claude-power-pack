# `scripts/` inventory - per-script history

A miniature inventory. `beta-tool.sh` exists in `scripts/` and has no entry
here, which is the drift issue #1013 is about.

## `alpha-tool`

- alpha-tool - the one entry this fixture carries.
