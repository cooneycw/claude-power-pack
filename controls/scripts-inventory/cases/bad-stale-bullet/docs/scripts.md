# `scripts/` inventory - per-script history

A miniature inventory carrying a BULLET entry for `beta-tool`, deleted from
`scripts/` and never removed from here. The forward check has nothing left to ask
about a script that no longer exists, so only a reverse check that reads bullets
as well as headings can see this - which is the gap Codex found in the first cut
of this gate, where the reverse direction examined headings alone.

<!-- scripts-inventory: non-script-sections: hooks -->

## `alpha-tool`

- alpha-tool - present in the tree.

## `hooks`

- `beta-tool` - a bullet entry for tooling that is gone.
