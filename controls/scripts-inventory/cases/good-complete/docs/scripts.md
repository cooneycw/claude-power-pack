# `scripts/` inventory - per-script history

A miniature inventory whose population matches its tree exactly, including one
declared non-script section, so the declaration path is exercised too.

<!-- scripts-inventory: non-script-sections: hooks -->

## `alpha-tool`

- alpha-tool - present in the tree, entered as a section.

## `hooks`

- `beta-tool` - entered as a BULLET under a grouped, declared non-script section,
  which is how the real inventory carries scripts that have no section of their own.
