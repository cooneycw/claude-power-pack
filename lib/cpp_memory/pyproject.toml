[project]
name = "cpp-memory"
version = "0.1.0"
description = "CPP common-memory: shared friction-knowledge ledger (bucket-2-plus)"
requires-python = ">=3.11"
dependencies = [
    "psycopg[binary]>=3.1",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
