# Grill-yourself - Issue #437 (Retire /skills:* family)

Date: 2026-07-03 · Trigger: >5 files · Verdict: **yes-with-caveats**

## Assumptions interrogated
1. **"Generated skills-* mirrors exist"** - FALSE. `/skills:*` were hand-authored
   command files (no `metadata.source`); no `manifests/skills/`, no
   `~/.claude/skills/skills-<verb>`. Task item 2 satisfied by "confirmed absent."
2. **skills-patterns.md is a mirror** - it is NOT; it's a knowledge skill about
   skill *activation/design*. Owner elected (ELI5 gate) to remove it anyway.
   -> also requires cleaning 2 index refs: `load-best-practices.md`, `best-practices.md`.
3. **No runtime caller** - FALSE. `documentation/pptx.md` invokes `/skills:list`
   + `/skills:add anthropics/skills@pptx`. Must reword to `npx skills`, not delete.
4. **deprecated-skills.yaml** - prune targets generated skills under
   `~/.claude/skills/`; commands are symlink-delivered. Entry is documentary +
   lists `skills-patterns`; commented for copied-install teardown.

## Caveats carried into implementation
- Reword (not delete) the pptx runtime references to `npx skills`.
- Preserve historical CHANGELOG (v7.1.0 release notes) + README release history;
  only edit LIVE feature/command references.
- No tests reference this surface (nothing to update).
