# Grill-yourself — Issue #439 (retire PreToolUse dangerous-command hook)

Date: 2026-07-03
Trigger: 9 files changed (> 5-file default threshold)
Mode: inline self-interrogation (fallback)
Note: run AFTER implement (out of order) — doubles as a pre-finish review.

## Q1. Is the load-bearing claim true — does native Claude Code actually cover the retired cases?
The hook guarded: force-push-to-main, `git reset --hard`, `rm -rf /`, recursive
`chmod`/`chown` on system dirs, `mkfs`/`fdisk`, kill-all, and SQL DROP/TRUNCATE/DELETE.
Native destructive-git auto-blocking (v2.1.154) covers the git cases; OS sandbox
covers the filesystem/system cases. This is trusted from the #417 ecosystem-review
decision (row 11 = RETIRE), not independently re-verified at runtime here.
-> CAVEAT (a): coverage claim inherited from the ratified epic decision.

## Q2. Did I preserve the PostToolUse secret-masking hook exactly?
Yes. Only the `PreToolUse` block was removed from hooks.json. Both PostToolUse
matchers (Bash + Read) -> hook-mask-output.sh are intact. JSON validated.

## Q3. What breaks on existing installs when the script disappears?
Scripts are symlinked from $CPP_DIR/scripts/*.sh; hooks.json is COPIED per project.
After merge+pull, `~/.claude/scripts/hook-validate-command.sh` dangles, and any
project whose copied hooks.json still has the PreToolUse block would run a missing
command -> non-zero exit -> BLOCK every Bash command.
-> Addressed: added guarded, user-confirmed /cpp:update Step 4.7 that removes the
   dangling symlink and strips the stale PreToolUse block (masking untouched).
-> CAVEAT (c): protection only applies once the user runs /cpp:update.

## Q4. Tier-2 detection still correct after shrinking the script loop?
status.md/update.md loops go 5->4 scripts; update.md gate is `-ge 3` -> still true.
doctor.md scripts loop and readiness table updated. hooks.json count text set to "2".

## Q5. SQL destructive-op heuristics — replaced?
No. Native blocking/sandbox do not cover DROP/TRUNCATE/DELETE. They were best-effort
regex warnings, not a guardrail. The review accepted this at RETIRE.
-> CAVEAT (b): documented explicitly in CHANGELOG as a deliberate tradeoff.

## Q6. Tests / gates?
No test references the hook or script inventory. `make verify` (lint+test+typecheck)
to be run at Step 7. Changes are markdown/JSON/shell only — no Python touched.

## Q7. Anything intentionally left?
docs/reviews/2026-07-02-ecosystem-review.md — dated historical record that produced
this decision. Left unchanged on purpose.

## Verdict: yes-with-caveats
Caveats to carry forward:
(a) native-coverage claim inherited from the epic decision, not runtime-verified here;
(b) SQL destructive-op heuristics have no native replacement (accepted tradeoff, documented);
(c) existing installs must run /cpp:update Step 4.7 to clear the dangling hook.
